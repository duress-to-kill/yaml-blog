# Temp files shouldn't be tracked
*.tmp

# Python bytecode is not actual code
*.pyc

# Swap happens
*.swo
*.swp
*.swn

# Posts should be managed in a different repository. They're content, not code.
posts/
pages/
app/static/images/

# This is reference content, it's not actually used by anything
app/templates/pure-layout-blog/
